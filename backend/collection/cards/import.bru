meta {
  name: import
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/cards/import
  body: file
  auth: inherit
}

body:file {
  file: @file(../example-files/ManaBox_Collection.csv) @contentType(text/csv; charset=utf-8)
}

settings {
  encodeUrl: true
  timeout: 0
}
