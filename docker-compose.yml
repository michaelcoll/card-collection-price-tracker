version: '3.8'

services:
  postgres:
    image: postgres:18
    restart: unless-stopped
    shm_size: 128mb
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5

  ccpt:
    image: ghcr.io/michaelcoll/card-collection-price-tracker:1
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgres://postgres:password@postgres:5432/postgres
      PORT: 8080
    ports:
      - "8080:8080"

  frontend:
    image: ghcr.io/michaelcoll/card-collection-price-tracker-frontend:1
    build:
      context: frontend/.
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "9797:80"
    depends_on:
      - ccpt

volumes:
  db_data:

